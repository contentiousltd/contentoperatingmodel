---
/**
 * Every list on a page that passes `components={{ ul: AccentList }}` to its
 * MDX content renders as the design system's accent bullets. The MDX source
 * stays a plain markdown list, which is what the Markdown twin wants.
 */
---
<ul class="c-bullets c-bullets--accent"><slot /></ul>
